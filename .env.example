# Kapadokya POI Sistemi - Çevre Değişkenleri
# Bu dosyayı .env olarak kopyalayın ve değerlerinizi girin

# Veritabanı Türü (mongodb, postgresql, json)
POI_DB_TYPE=mongodb

# Veritabanı Bağlantı Dizesi
# MongoDB için:
POI_DB_CONNECTION=mongodb://localhost:27017/
POI_DB_NAME=poi_cappadocia

# PostgreSQL için:
# POI_DB_CONNECTION=postgresql://kullanici:sifre@localhost/poi_db

# JSON dosyası kullanıyorsanız yukarıdaki değişkenleri ayarlamanıza gerek yok

# API Sunucu Ayarları
FLASK_HOST=0.0.0.0
FLASK_PORT=5505
FLASK_DEBUG=false

# Opsiyonel: Log seviyesi (DEBUG, INFO, WARNING, ERROR)
POI_LOG_LEVEL=INFO

# Opsiyonel: Test verileri kullanımı (true/false)
POI_USE_TEST_DATA=false

# Opsiyonel: Cache ayarları
POI_CACHE_ENABLED=true
POI_CACHE_TTL=3600

# Opsiyonel: Yükseklik API ayarları
ELEVATION_API_ENABLED=true
ELEVATION_API_TIMEOUT=30

# Opsiyonel: OSM ayarları
OSM_GRAPH_RADIUS_KM=10
OSM_NETWORK_TYPE=walk